apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
data:
  # Включаем GitHub вебхуки
  webhook.github.secret: "$argocd-webhook-secret:webhook.github.secret"
  # Автосинк включён через манифесты Application


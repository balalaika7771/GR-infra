apiVersion: v1
kind: Secret
metadata:
  name: argocd-webhook-secret
  namespace: argocd
type: Opaque
stringData:
  # Используйте это значение как secret при настройке GitHub Webhook
  webhook.github.secret: "change-me-strong-secret"


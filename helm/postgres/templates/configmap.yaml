apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postgres.fullname" . }}-init-scripts
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
data:
  init.sql: |
{{- range .Values.initScripts }}
    {{ . | nindent 4 }}
{{- end }}



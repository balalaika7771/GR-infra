apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "purpur-shard.fullname" . }}-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "purpur-shard.name" . }}


